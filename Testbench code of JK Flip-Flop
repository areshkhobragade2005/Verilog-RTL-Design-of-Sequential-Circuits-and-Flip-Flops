reg clk, j, k;
wire q;

jk_ff uut (
    .clk(clk),
    .j(j),
    .k(k),
    .q(q)
);

always #5 clk = ~clk;

initial begin
    clk = 0;
    j = 0; k = 0;

    #10 j = 1; k = 0; // Set
    #10 j = 0; k = 1; // Reset
    #10 j = 1; k = 1; // Toggle

    #20 $finish;
end
